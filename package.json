{
  "name": "dsh-migrate-openclaw",
  "version": "0.2.0",
  "description": "DSH plugin: migrate OpenClaw data into DeepSeek Harness — scan an OpenClaw data directory, import the persona core into the global instruction layer, memories as workspace Markdown with an index, and sessions as native DSH session logs.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./client": "./src/client.js",
    "./package.json": "./package.json"
  },
  "dsh": {
    "client": {
      "inject": ["slots", "sessions", "locale"],
      "platform": "web",
      "immediately": true
    }
  },
  "files": [
    "src"
  ],
  "license": "MIT"
}
